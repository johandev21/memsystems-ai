// Next.js module-marker that prevents server-only modules from being
// imported on the client. In a Node test environment there is no client
// boundary to enforce, so the module is a no-op.
export {};
